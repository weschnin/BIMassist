<Window x:Class="BIMassist.Views.LevelPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ebene auswählen"
        Height="360"
        Width="390"
        MinHeight="320"
        MinWidth="340"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptTextBlock"
                   Grid.Row="0"
                   Text="Ebene aus dem aktuellen Revit-Dokument auswählen"
                   FontWeight="Bold"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10" />

        <TextBox x:Name="SearchTextBox"
                 Grid.Row="1"
                 Margin="0,0,0,10"
                 TextChanged="SearchTextBox_TextChanged" />

        <ListBox x:Name="LevelListBox"
                 Grid.Row="2"
                 MouseDoubleClick="LevelListBox_MouseDoubleClick"
                 SelectionMode="Single">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="{Binding Name}"
                                   FontWeight="SemiBold" />
                        <TextBlock Grid.Column="1"
                                   Text="{Binding ElevationText}"
                                   Foreground="DimGray"
                                   Margin="16,0,0,0" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="Übernehmen"
                    Width="100"
                    Margin="0,0,10,0"
                    Click="OkButton_Click" />
            <Button Content="Abbrechen"
                    Width="100"
                    Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
