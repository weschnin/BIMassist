<Window x:Class="BIMassist.Einstellungen"
        x:Name="WindowEinstellungen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d" Topmost="True" Height="301.2" Icon="/BIMassist;component/Resources/settings_32px.png"
        Title="Einstellungen" Width="483.1">

    <Grid Margin="0,0,3.8,0" Height="266" VerticalAlignment="Top">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="44.8"/>
            <RowDefinition Height="90.4"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="205.6"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="textBlockExportpfad" HorizontalAlignment="Left" Margin="20,14.2,0,0" TextWrapping="Wrap" Text="Dateipfad für Autospeicherung" VerticalAlignment="Top" Height="16" Width="181" Grid.Row="1"/>
        <TextBox x:Name="textBoxExportPfad" Text="{Binding EigeneDaten, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" HorizontalAlignment="Left" Height="23" Margin="20,35.2,0,0" VerticalAlignment="Top" Width="445" Grid.Row="1"/>
        <Button x:Name="button" Command="{Binding OpenOwnDataCommand}" CommandParameter="True" Content="Wählen" HorizontalAlignment="Left" Height="23" Margin="411,63.2,0,0" VerticalAlignment="Top" Width="54" Grid.Row="1"/>
        <Button x:Name="buttonClose" Content="Übernehmen und Schließen" Command="{Binding CloseSettingsWindow}" CommandParameter="{Binding ElementName=WindowEinstellungen}" HorizontalAlignment="Left" Height="23" Margin="20,34.8,0,0" VerticalAlignment="Top" Width="164" Grid.Row="3" />
        <CheckBox x:Name="checkBoxBestätingBGKZUweisung" Content="Bestätigung bei BGK-Zuweisung" IsChecked="{Binding BestaetigungBGKZUweisung, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" HorizontalAlignment="Left" Margin="20,37.8,0,0" Grid.Row="2" VerticalAlignment="Top" Width="221" Height="15"/>
        <CheckBox x:Name="checkBoxAutoSave" Content="Nach Eintragung der Baugruppenkennzeichnung Familien speichern" IsChecked="{Binding FamilyAutoSave, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" HorizontalAlignment="Left" Margin="20,17.8,0,0" Grid.Row="2" VerticalAlignment="Top" Width="419" Height="15"/>
    </Grid>
</Window>
