<Window x:Class="BIMassist.Views.NamePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SectionBox benennen" Height="150" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Bezeichnung für die SectionBox:"/>
        <TextBox x:Name="NameBox" Grid.Row="2" MinWidth="260" Margin="0,2,0,0"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Abbrechen" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Speichern" IsDefault="True" Click="OnSaveClick"/>
        </StackPanel>
    </Grid>
</Window>