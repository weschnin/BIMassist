<Window x:Class="BIMassist.Views.MaterialPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Material auswählen"
        Height="520"
        Width="420"
        MinHeight="420"
        MinWidth="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Material aus dem aktuellen Revit-Dokument auswählen"
                   FontWeight="Bold"
                   Margin="0,0,0,10" />

        <TextBox x:Name="SearchTextBox"
                 Grid.Row="1"
                 Margin="0,0,0,10"
                 TextChanged="SearchTextBox_TextChanged" />

        <ListBox x:Name="MaterialListBox"
                 Grid.Row="2"
                 MouseDoubleClick="MaterialListBox_MouseDoubleClick"
                 SelectionMode="Single" />

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="Übernehmen"
                    Width="100"
                    Margin="0,0,10,0"
                    Click="OkButton_Click" />
            <Button Content="Abbrechen"
                    Width="100"
                    Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
